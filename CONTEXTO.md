# CONTEXTO.md — ZappoStore

> Archivo de handoff. Es la **única fuente de verdad** del pre-proyecto (ahora sí versionado en GitHub: `github.com/jesusperd/zappostore`). Para retomar en otra sesión: abrí un chat nuevo, subí el proyecto (o al menos `src/App.jsx`, `schema.sql` y este archivo) y pegá el bloque **"Resumen para retomar"**.

---

## 🔜 Para la próxima sesión — arrancar por acá

Estado exacto en el que quedó todo al cerrar (2026-07-17, v2.1): **la migración a Supabase está 100% completa, incluida la última pieza (Storage de fotos).** No queda ningún dato del flujo operativo diario en memoria pura — login, vendedores, clientes, cajas, pedidos, pagos y fotos de diseño persisten de verdad, con RLS verificada tabla por tabla y `schema.sql` (v0.7) como fuente de verdad completa y al día.

- ✅ Todo lo anterior (v1.0–v2.0: Auth, manager de apertura, clientes, cajas, núcleo de pedidos/pagos con reversión) + **v2.1: Storage real de fotos de diseño**, bucket privado `diseno-fotos` con URLs firmadas, verificado end-to-end (subir, servir, borrar sin dejar rastro).
- ✅ Todo commiteado y pusheado a `main` (revisar `git log` para el último hash).
- ⏭️ **Lo que sigue es una conversación distinta, no más migración**: qué hace falta para llevar esto a producción real con el cliente (dominio, hosting del frontend, límites del plan gratis de Supabase, backups, monitoreo). Si esa conversación no llegó a cerrarse en esta sesión, retomarla es el primer paso de la próxima.
- Pendientes de producto (no de infraestructura): Inventario/Stock, Gastos/Estadísticas, filtrar Resumen/Reportes por fecha/turno.
- El servidor de dev (`npm run dev`) no queda corriendo entre sesiones — arrancalo de nuevo vos o pedímelo.
- Los 2 usuarios sembrados (master `1234567`, vendedor `95414372`) siguen activos en la base — no hace falta recrearlos.

---

## Resumen para retomar (pegá esto en un chat nuevo)

Estoy construyendo **ZappoStore**, un sistema POS + producción textil (remeras, gorras, estampado/bordado) inspirado en la app venezolana **Fina**: súper simple, bimoneda (USD/Bs con tasa dinámica), evolucionando hacia un ERP/Kanban de producción para tablet/desktop. Trabajamos en **modo plan** y **siempre** actualizamos `CONTEXTO.md` al final. Stack: **React (Vite) + Tailwind + Supabase (Postgres + Auth + Storage)**, repo en `github.com/jesusperd/zappostore`.

**Estado actual (v2.1): el backend es 100% real, nada queda en memoria pura.** Login (Supabase Auth, email sintético `cedula@zappostore.local`), vendedores (con manager de apertura vía Edge Function `create-vendor`), clientes (directorio compartido), cajas (apertura/cierre por turno), y el núcleo completo de Vender/Seguimiento (`pedidos`+`pedido_items`+`pedido_item_diseno`+`pedido_item_fotos`+`pagos`, con Kanban de procesos y edición) corren contra Postgres real con RLS verificada tabla por tabla. El pago es mixto por pedido (N abonos, distintos métodos) con un ledger append-only que soporta reversión exacta (corregir un pago mal cargado sin dejar dinero fantasma en Caja) — la pieza más delicada de todo el proyecto, auditada primero en memoria (v0.8) y reverificada después de la migración con un test end-to-end contra la base real. Las fotos de diseño suben a un bucket privado de Storage (`diseno-fotos`) servido con URLs firmadas. La comanda imprime en formato ticket térmico 58mm y muestra un checklist real de procesos. `schema.sql` (v0.7) es la fuente de verdad completa — alguien podría recrear la base entera desde cero solo con ese archivo (tablas + RLS + Storage + seed).

Módulos que siguen 100% de lectura sobre datos ya reales pero sin escritura propia: Resumen Financiero y Reportes (leen `sales`/`payments`, calculan histórico global, no filtran por fecha todavía). Pendiente: Inventario/Stock, Gastos/Estadísticas, y — la pregunta del momento — qué hace falta para llevar esto a producción real con el cliente. Detalle completo de decisiones y pendientes abiertos más abajo; el historial de cómo se llegó hasta acá está en el Changelog.

---

## Qué es ZappoStore
POS + control de producción para un taller textil. Toma pedidos, rutea el trabajo (taller/diseño) o venta directa, cobra en USD/Bs, y da seguimiento y trazabilidad de pedidos, clientes y vendedores. Referencia de UX: **Fina (Venezuela)**.

## Decisiones cerradas
- **Roles:** `master` (supervisor) y `vendedor`. Login de master **aparte** (pantalla separada, mismo mecanismo de auth por debajo).
- **Permisos:** vendedor ve solo sus ventas y, de un cliente, solo lo suyo. Master ve todo y gestiona vendedores.
- **Autenticación real (Supabase Auth):** el login dejó de ser credenciales hardcodeadas/en memoria. `login(cedula, password)` llama a `supabase.auth.signInWithPassword({ email: cedula@zappostore.local, password })` y después busca el perfil en `vendedores` por `auth_user_id` para saber el rol/nombre/estado activo. Si `!active`, se hace `signOut()` inmediato y error "Vendedor inactivo". Si la cédula pertenece a un rol distinto al de la pantalla usada (ej. vendedor intentando entrar por "Supervisor"), se rechaza aunque la contraseña sea correcta. La sesión de Supabase persiste sola (JWT en `localStorage`) — a diferencia de antes, refrescar la página ya no desloguea (hay un `useEffect` que llama `supabase.auth.getSession()` al montar y restaura la sesión). Ya **no existe** el viejo comportamiento de "auto-crear un vendedor al tipear cualquier cédula nueva" — con auth real eso sería un agujero de seguridad.
- **Manager de apertura (alta de vendedores real):** desde "Vendedores" (solo master) se crea un vendedor con nombre/cédula/clave. Como el proyecto tiene **self-signup deshabilitado a nivel de instancia** (decisión correcta de seguridad — no cualquiera con la anon key debería poder crear cuentas), la creación de la cuenta de Auth **no puede hacerse desde el navegador**. Se resolvió con una **Edge Function** (`supabase/functions/create-vendor/index.ts`): corre en el servidor, usa la Service Role Key (nunca sale de ahí), verifica que quien invoca sea un master activo (leyendo su propio JWT), crea el usuario de Auth con `admin.createUser({ email_confirm: true })` y la fila en `vendedores` en un solo paso atómico (si falla el insert, borra el auth user recién creado — sin huérfanos). El front la llama con `supabase.functions.invoke("create-vendor", { body: {...} })`, que automáticamente reenvía el JWT del master logueado.
- **Moneda:** USD base, Bs derivado por **tasa dinámica** editable en el header.
- **Producto:** cada ítem tiene **Talla** (chips S–XXL) + **Cantidad** (input numérico simple, sin botones +/-) + **Color** libre (texto, ej. "Gris plomo"). Se eliminó el concepto de Formato (`talle`/`peso`/`cantidad`) y la paleta de colores predefinida — eran fricción innecesaria en el mostrador.
- **Layout:** menú lateral (`Sidebar`) es una columna **fija** (`<aside>` w-72, siempre visible), no un drawer/modal. Sin botón hamburguesa ni overlay — pensado para tablet/desktop, uso de taller. Ancho ajustable a gusto.
- **Operación y procesos (reemplaza "Destinos"):** cada producto tiene `tipo_operacion` (`venta` directa | `presupuesto`) y dos listas independientes de procesos que necesita: `procesos_taller` (espera del producto / corte / confección / planchado) y `procesos_diseno` (DTF / sublimación / bordado / vinil). Se eligen en 3 desplegables (acordeón) dentro de la edición del producto: Operación, Taller, Diseño. Un producto puede tener 0, 1 o varios procesos de cada tipo — no hay obligación de elegir nada para poder guardarlo (a diferencia del viejo "Destino", que exigía al menos uno).
- **Presupuesto vs. Venta:** si algún producto del carrito tiene `tipo_operacion = "presupuesto"`, el botón inferior de Vender cambia de "Cerrar venta" (verde) a **"Imprimir Presupuesto"** (azul). Al tocarlo se abre la comanda para imprimir, pero **no** se llama a `onSaved`: no se crea un pedido firme, no se registra en Caja ni en Seguimiento. El vendedor sigue en la pantalla de Vender después de imprimir/cerrar el modal.
- **Kanban de producción (en Seguimiento):** al abrir "Gestionar productos" de una venta, cada producto muestra 2 bloques lado a lado (Taller / Diseño) con los procesos elegidos como chips. Cada chip es clicable: alterna entre **pendiente** (gris) y **completado** (verde), tocando arrays separados de progreso (`procesos_taller_done` / `procesos_diseno_done`, distintos de los arrays de "qué procesos aplican") y dejando entrada en `audit_log`.
- **Zonas de diseño:** `ZonePicker` con pestañas **Frente / Espalda / Mangas** (nomenclatura textil profesional, en **visión de espejo**: la izquierda visual del dibujo es la derecha real de la prenda). Frente: hombro/sup izq-der, pecho izq-der, centro pecho, abdomen (6 zonas numeradas). Espalda: alta, media, baja (3 zonas físicas en el dibujo). Mangas: izquierda, derecha (vista lateral con `ShirtSideSVG`). Cada zona se marca con un círculo numerado sobre el dibujo (`ShirtSVG`/`ShirtSideSVG`) y además hay una lista tipo grid debajo para tocar sin apuntar al dibujo. **Regla espalda completa (bidireccional, resuelta):** "Esp. Completa" ya no existe como zona del dibujo — es un 4º botón solo en la lista, con estado 100% calculado (`ESPALDA_PARTS.every(...)`). Marcar alta+media+baja la enciende sola; tocarla directamente marca o desmarca las 3 partes juntas, en ambos sentidos.
- **Fotos y notas de diseño, por pestaña:** cada producto tiene `designData = { frente, espalda, mangas }`, cada una con su propio array de `photos` (multi-carga, miniatura + botón "X" para borrar) y su propio `notes` — independiente de las zonas puntuales marcadas en el dibujo. La pestaña activa de `ZonePicker` es controlada desde `ItemEditForm` y muestra el conteo de fotos (ej. "Espalda (2 📸)"). La comanda impresa agrupa fotos/notas por pestaña.
- **Estado por producto:** `en_curso` → `en_espera_retiro` → `cerrado`.
- **Estado de venta (derivado):** en curso / en espera de retiro / cerrada. **Regla dura:** la venta solo cierra cuando **todos** sus productos están `cerrado`.
- **Pago (mixto por pedido):** ya no existe pago por ítem ni el switch junto/fraccionado. El pago es siempre a nivel de **pedido completo**: `orderPayments` es un array donde se pueden agregar **N abonos con métodos distintos** (Efectivo $/Bs, Tarjeta, Pago Móvil con referencia obligatoria) hasta cubrir el total. El panel (`PaymentPanel`) muestra el total en USD/Bs, la lista de abonos agregados (con botón para quitar uno), y un indicador grande de **"Restante por pagar"** (USD + Bs, en tiempo real) que cambia a **"Pagado completo"** en verde al llegar a $0. **Integridad del ledger:** cada abono agregado queda vinculado por `orderPaymentId` a su evento correspondiente en el ledger de Caja (`payments`); si durante una edición se quita un abono que ya estaba guardado (ej. corregir un monto mal tipeado), `saveSale` genera automáticamente un **evento de reversión negativo en la misma caja donde se cobró originalmente** (buscado por `orderPaymentId`, no por la caja actual de quien edita) — así Caja nunca conserva dinero que la venta ya no muestra. Verificado con un script de simulación (8 escenarios, ver Changelog v0.8).
- **Cliente:** además de nombre y teléfono, ahora se captura **Cédula/RIF** y **Dirección**. `findOrCreateClient` los persiste al crear el cliente y los actualiza (si vienen no vacíos) en pedidos posteriores del mismo cliente. Al cerrar, si el teléfono ya existe se reutiliza; si no, se crea. No se vuelve a pedir dentro del mismo pedido.
- **Comanda como hoja de ruta (`ComandaModal`):** los procesos de Taller/Diseño de cada ítem se listan como **checklist** (`ProcesoChecklist`), cruzando `procesos_taller`/`procesos_diseno` con sus `_done` — icono `Check` verde si ya se completó, `Clock` gris si sigue pendiente (mismo criterio que el Kanban de Seguimiento, pero en formato lectura/impresión). El bloque de cliente (nombre/cédula/teléfono/dirección, o "Cliente Ocasional") vive en un recuadro `bg-slate-50` destacado. El bloque de pago muestra Total Pedido (USD+Bs), Detalle de Abonos (uno por línea de `orderPayments`), Total Pagado, y un recuadro final "Pagado Completo" (verde) o "SALDO PENDIENTE" (rojo, con el monto en USD y Bs).
- **Impresión térmica 58mm:** al llamar `window.print()`, un `<style>` local con `@media print` fuerza `@page { size: 58mm auto; margin: 0 }`, aísla solo el contenido de la comanda (clase `zs-print-ticket`, con el truco clásico `body * { visibility:hidden }` + `.zs-print-ticket, .zs-print-ticket * { visibility:visible }` + `position:absolute`), reduce las fuentes a 9–12px, limita imágenes a 36px, y oculta header/tabs/botones vía `print:hidden` (variante nativa de Tailwind, sin config extra). La vista en pantalla (modal centrado, bordes redondeados, colores) queda intacta — solo cambia lo que sale por impresora.
- **Trazabilidad:** `audit_log` por pedido (quién/qué/cuándo), visible en la pestaña "Cambios" de la comanda.
- **Edición:** vendedor y master pueden editar pedidos en cualquier estado; cada cambio se audita.
- **Caja (turno):** cada pago (nuevo o abono) queda registrado como **evento** en un ledger (`payments`), no como un campo que se sobreescribe — es lo que permite saber cuánto entró en el turno actual vs. en otro momento. Caja cuenta los 4 métodos de pago. Al abrir turno el vendedor declara fondo inicial en efectivo ($ y Bs); al cerrar declara efectivo contado y el sistema calcula la diferencia (sobrante/faltante) contra lo esperado (fondo + cobros en efectivo del turno). El saldo "comprometido por cobrar" del turno se calcula **solo** sobre pedidos tomados durante ese turno (no histórico completo del vendedor). El master no opera caja propia: su vista es de supervisión global (todas las cajas).
- **Logo/marca:** sapo (transparente, sin recuadro) + wordmark "ZappoStore" en rojo LED suave con leve flicker.
- **CONTEXTO.md** se regenera en cada tanda de cambios (regla permanente).

## Estado de módulos
| Módulo | Estado |
|---|---|
| Login vendedor + master aparte | ✅ Hecho — **real, Supabase Auth** |
| Menú lateral **fijo** (columna `<aside>`, no drawer) | ✅ Hecho (con secciones "Próximamente") |
| Home / menú | ✅ Básico |
| Vender (carrito, Talla+Cantidad+Color libre, zonas de diseño con tabs Frente/Espalda/Mangas + espejo, 3 desplegables Operación/Taller/Diseño, estado, cliente con cédula/dirección, pago mixto por pedido, botón Presupuesto) | ✅ Hecho — **real, Supabase**, incluidas las fotos (Storage) |
| Comanda: checklist de procesos, bloque de cliente destacado, bloque de pago detallado, impresión térmica 58mm, pestaña Historial de cambios | ✅ Hecho |
| Seguimiento por venta + gestión por producto + **Kanban de producción** (chips Taller/Diseño clicables) + regla de cierre | ✅ Hecho — **real, Supabase** |
| Clientes (auto-registro, lista, ficha con gráfico) | ✅ Hecho — **real, Supabase** (perfil y lista visible ya reflejan datos persistidos) |
| Vendedores (alta/edición/activar, ficha con gráfico) — solo master | ✅ Hecho — **real, Supabase** (manager de apertura vía Edge Function) |
| Edición de pedidos en cualquier estado + audit | ✅ Hecho — **real, Supabase** |
| **Caja (abrir/cerrar por turno, ledger de pagos, cuadre de efectivo, vista global master)** | ✅ Hecho — **real, Supabase** (el cuadre ya lee el ledger real, no memoria) |
| **Resumen Financiero** (KPIs: ingresos totales histórico, cuentas por cobrar, ticket promedio + detalle de CxC) — solo master | ✅ Hecho — lee datos reales (histórico global, no filtra por fecha todavía) |
| **Reportes / Estadísticas** (ingresos por mes, Top 5 clientes, Top 5 vendedores) — solo master | ✅ Hecho — lee datos reales |
| **Inventario / Stock** (entradas, salidas, por talla/color) | ⏭️ Después (ganchos ya en schema) |
| Gastos / Estadísticas / Marketing | 🔜 Más adelante |
| **Supabase: núcleo completo** (`vendedores`, `clientes`, `cajas`, `pedidos`, `pedido_items`, `pedido_item_diseno`, `pagos`, `audit_log`) con RLS verificada | ✅ Hecho |
| Supabase: Storage real para fotos de diseño (`pedido_item_fotos`, bucket `diseno-fotos`) | ✅ Hecho |

## Modelo de datos (ver `schema.sql`, v0.6 — alineado al modelo en memoria, corrido contra el proyecto real)
Tablas: `vendedores` (+ `auth_user_id → auth.users`), `clientes` (+ `cedula`/`direccion`), `exchange_rates`, `products` (sin `formato`), `cajas`, `pedidos` (folio autogenerado por trigger, sin `pay_mode`/`subtotal`), `pedido_items` (talla+cantidad+color, `operacion_tipo`, `procesos_taller`/`procesos_diseno` + `*_done`), `pedido_item_diseno` (notas por zona Frente/Espalda/Mangas), `pedido_item_fotos` (fotos multi-carga por zona — tabla lista, todavía sin usar desde el front), `pagos` (ledger con `order_payment_id` + `is_reversal`), `audit_log`, `inventory_movements` (gancho).
Vistas: `v_historial_cliente`, `v_historial_vendedor`, `v_pedido_saldo` (cobrado/balance por pedido, derivado de `pagos`).
Relación clave: `pedidos.vendedor_id` + `pedidos.cliente_id` habilita ambos historiales. `pedidos.caja_id` y `pagos.caja_id` habilitan el cuadre de Caja por turno; `pagos.order_payment_id` vincula un abono con su eventual reversión (mismo `caja_id` que el pago original, no el de quien edita).
**RLS:** `vendedores`/`clientes` con políticas por rol activo; `cajas`/`pedidos`/`pagos` con dueño-o-master (`my_vendedor_id()`, `is_active_master()`); `pedido_items`/`pedido_item_diseno` heredan el permiso del pedido padre (`owns_pedido()`, `owns_pedido_item()` — funciones `security definer` reutilizables). `pagos` solo tiene políticas de select/insert (nunca update/delete — el ledger es append-only a propósito, ver v0.8).

## App.jsx → tablas (ya no queda nada "en memoria" del núcleo)
`vendors→vendedores`, `clients→clientes`, `sales→pedidos (+pedido_items, pedido_item_diseno, pagos)`, `sale.audit→audit_log`, `payments→pagos`, `cajas→cajas`. Los `orderPayments` de un pedido se reconstruyen en `mapSale`/`reconstructOrderPayments` agrupando las filas de `pagos` por `order_payment_id` y sumando `amount_usd` — un pago revertido netea a ~0 y queda excluido automáticamente, sin necesitar una tabla aparte para "el estado actual". `designData` persiste solo las **notas** por zona (`pedido_item_diseno`); las **fotos** siguen solo en memoria durante la sesión (Storage real es la tarea pendiente). Patrón general: cada tabla tiene un `map*()` (fila de Postgres → forma que ya esperaba el front) y un `load*()` (fetch + `setState`, disparado por `useEffect` al loguearse); las mutaciones (`saveSale`, `setItemState`, `toggleProceso`, `openCaja`, `closeCaja`, `addVendor`, `findOrCreateClient`) son `async`, escriben directo a Supabase, y al final refrescan el cache local (`loadSales()`+`loadPayments()` tras `saveSale`, patch local directo para las demás).

## Cómo correr el proyecto
```bash
npm install
cp .env.example .env   # completar VITE_SUPABASE_URL y VITE_SUPABASE_ANON_KEY (Dashboard -> Settings -> API)
npm run dev             # abre http://localhost:5173
```
Estructura:
```
zappostore/
├─ index.html
├─ package.json  vite.config.js  tailwind.config.js  postcss.config.js
├─ schema.sql            # esquema Supabase/Postgres (v0.6, ya corrido contra el proyecto real)
├─ .env.example          # variables necesarias (VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY)
├─ .env                  # tus credenciales reales — gitignored, nunca se commitea
├─ CONTEXTO.md           # este archivo
├─ README.md
├─ supabase/
│  ├─ config.toml        # config del CLI de Supabase
│  └─ functions/
│     └─ create-vendor/  # Edge Function: alta real de vendedores (manager de apertura)
└─ src/
   ├─ main.jsx  index.css
   ├─ App.jsx            # toda la app (login/vendedores ya usan Supabase; el resto en memoria)
   ├─ assets/logo.js     # logo del sapo (data URI)
   └─ lib/
      ├─ supabase.js     # cliente Supabase real (Singleton, vía globalThis)
      └─ store.js        # persistencia local opcional (localStorage) — sin usar hoy
```
> Nota de arquitectura: hoy las pantallas viven en `App.jsx`. Próximo refactor sugerido: separar a `src/screens/*` y `src/components/*` (los bloques ya están claramente delimitados por función).

**Deploy de la Edge Function** (una sola vez, o cada vez que se edite `supabase/functions/create-vendor/index.ts`):
```bash
npx supabase login
npx supabase link --project-ref aqtaphhhomlbafelekiz
npx supabase functions deploy create-vendor
```

## Pendientes / preguntas abiertas
- **Migrar el resto de la app a Supabase** (Vender, Seguimiento, Caja, Clientes, Resumen, Reportes): hoy solo login/vendedores son reales, todo lo demás sigue en `useState`. Es la tarea más grande que queda — especialmente el ledger de pagos con reversión (v0.8), que necesita el mismo cuidado que tuvo en memoria.
- **RLS granular por rol** para el resto de las tablas (`clientes`, `pedidos`, `pedido_items`, `pagos`, `cajas`, etc.) — hoy solo tienen RLS "todo bloqueado, sin políticas" (baseline de seguridad) porque la app todavía no las usa vía Supabase; hay que escribir las políticas reales (vendedor ve solo lo suyo, master ve todo) cuando se migren esas pantallas.
- (resuelto en v2.1 — ver Changelog) ~~Storage real para fotos de diseño~~.
- Ancho del sidebar (`w-72`) queda abierto a ajustar según cómo se vea en el monitor real del cliente.
- **Resumen Financiero** hoy calcula sobre el histórico global (todas las cajas/vendedores, todo el tiempo), no por turno o por día — si se quiere un KPI del día se puede filtrar `sales`/`payments` por fecha, reusando el mismo patrón de `inRange`/`PeriodTabs` que ya existe en `CajaScreen`.
- **Edición de ventas después de cerrar el turno:** si se edita una venta cuya caja ya fue cerrada (agregando/quitando pagos), los totales *en vivo* de esa caja (`cajaTotals`, arriba en `CajaBreakdown`) reflejan el cambio, pero el cuadre congelado al momento del cierre (`caja.expectedCashUSD`/`caja.varianceUSD`, guardado en el objeto `caja`) **no se recalcula** — puede quedar desalineado del total en vivo. Es un comportamiento heredado del diseño original (no es nuevo de esta tanda); si se vuelve un problema real, la solución sería bloquear la edición de pagos de ventas que pertenecen a una caja ya cerrada, o versionar el cuadre.
- **Stock**: reglas de descuento por venta/producción, por talla/color.
- ¿Seguimiento por producto (además de por venta) en el tablero? (hoy es por venta con gestión de productos adentro, incluido el nuevo Kanban de procesos).
- Egresos/retiros de caja durante el turno (gastos en efectivo) — no implementado, quedó fuera de esta tanda.
- ¿Master debería poder operar su propia caja (vender directamente)? Hoy su vista de "Caja" es solo supervisión global, no opera turno propio.
- Un "Presupuesto" impreso no queda registrado en ningún lado (ni Seguimiento ni Caja) — es intencional (evita inflar números), pero no hay forma hoy de recuperar/reimprimir un presupuesto viejo ni de "convertirlo" en venta real con un clic.

## Credenciales
Ya no hay credenciales "demo" hardcodeadas — son cuentas reales de Supabase Auth. Por seguridad, las contraseñas **no se documentan en este archivo** (es un repo en GitHub). Hay 2 cuentas sembradas:
- **Master:** cédula `1234567` (clave conocida por el dueño del proyecto).
- **Vendedor:** cédula `95414372` (clave conocida por el dueño del proyecto).

Nuevos vendedores se crean desde la pantalla "Vendedores" (solo master) — el manager de apertura. Ya no existe el login "cualquier cédula funciona" de las versiones anteriores.

## Changelog
- **2026-07-17** (cont.) — v2.1: **Storage real para fotos de diseño — cierra el último hueco de la migración.** Bucket privado `diseno-fotos` (creado por SQL en `schema.sql`, 2MB máx por archivo, solo JPEG/PNG/WebP), servido con URLs firmadas (nunca público). `ItemEditForm`: `draft.designData[vista].photos` pasó de ser un array de strings (data URI o URL, ambiguo) a un array de `{ path, url }` — `path: null` significa "todavía no subida esta sesión". `saveSale` extiende `syncDesignData` (antes `syncDesignNotes`, ahora también fotos): sube las que tienen `path: null` (convierte el data URI ya comprimido por `optimizeImage` a Blob con `fetch(url).then(r=>r.blob())`, sin re-comprimir), borra de Storage + `pedido_item_fotos` las que ya no están en la lista, y limpia también las fotos de items eliminados durante una edición (evita archivos huérfanos). `loadSales` genera las URLs firmadas en un solo batch (`createSignedUrls`) para todas las fotos de todas las ventas cargadas, no una por una. RLS del bucket: cualquier vendedor/master activo puede subir/ver/borrar (mismo criterio de "directorio compartido" que `clientes`/`audit_log`); RLS de `pedido_item_fotos` hereda del pedido padre (`owns_pedido_item`, igual que `pedido_item_diseno`). **Verificado end-to-end:** subir una foto real, generar su URL firmada y confirmar que responde 200, confirmar que un usuario sin login NO puede listar el bucket, borrarla y confirmar que no queda ni el archivo en Storage ni la fila en la tabla. **También se aprovechó para poner al día `schema.sql`**: llevaba desde v0.5 sin las políticas RLS reales que se fueron aplicando en vivo toda la noche (funciones `security definer`, políticas de las 8 tablas del núcleo) — quedaron aplicadas en la base pero nunca se habían vuelto a escribir en el archivo commiteado. Ahora `schema.sql` (v0.7) es de nuevo la fuente de verdad completa: alguien podría recrear la base entera desde cero solo con este archivo.
- **2026-07-17** — v2.0: **migración del núcleo completa** — `pedidos`+`pedido_items`+`pedido_item_diseno`+`pagos` (Vender, Seguimiento, edición, Kanban de procesos) pasaron de `useState` en memoria a Supabase real. Es la pieza más grande y delicada de toda la migración (ahí vive la lógica anti-fuga-de-dinero de v0.8) y la última del núcleo — con esto, salvo las fotos de diseño (Storage, pendiente), **nada del flujo operativo diario se pierde al refrescar la página.**
  - **`saveSale` reescrito** (crear y editar) con diff explícito de items (agregado→insert secuencial para mapear id local↔id real; quitado→delete; existente→update) y de pagos (mismo patrón add/remove de v0.8, ahora escribiendo contra la tabla `pagos` real: un pago nuevo es un insert, uno quitado genera una fila de reversión negativa en la MISMA caja donde se cobró originalmente, buscada por `order_payment_id`). `setItemState`/`toggleProceso` pasaron a `async`, actualizan `pedido_items` + insertan en `audit_log`.
  - **`mapSale`/`loadSales`**: reconstruye `data.items`, `data.client` y — el punto más delicado — `data.orderPayments` a partir del ledger append-only de `pagos`: agrupa por `order_payment_id`, suma `amount_usd`, y descarta los que netean a ~0 (revertidos). Así el front nunca necesita saber "qué pagos siguen vigentes" aparte — se deriva solo del ledger, igual que en memoria.
  - **`payments` (el array que usan `cajaTotals`/`CajaBreakdown`/Resumen/Reportes) se mantiene como cache local sincronizado** (`loadPayments`, mismo patrón que `sales`/`clients`/`cajas`) en vez de mutarse a mano — así ninguna pantalla que ya consumía ese array necesitó cambiar una sola línea.
  - **Folio autogenerado por Postgres**: el contador `useRef` del navegador para `ZS-0001` generaba colisiones garantizadas con más de un vendedor concurrente — se reemplazó por una secuencia real (`pedidos_folio_seq`) + trigger (`set_pedido_folio`) en `schema.sql` (v0.6).
  - **Bug real encontrado por el test end-to-end, no por revisión manual**: `PaymentPanel` generaba el `id` de cada abono con el helper corto `uid()` (ej. `"j616jua"`), pero `pagos.order_payment_id` es `uuid` en Postgres — el insert de CUALQUIER pago iba a fallar en producción. Se agregó `newUuid()` (`crypto.randomUUID()`) específicamente para ese caso; `uid()` se dejó igual donde solo se usa como key local/temporal (nunca viaja a una columna `uuid`).
  - **RLS**: `owns_pedido(uuid)`/`owns_pedido_item(uuid)` (funciones `security definer` reutilizables) scopean `pedidos`/`pedido_items`/`pedido_item_diseno` por dueño-o-master; `pagos` solo tiene políticas de **select/insert** (nunca update/delete — refuerza a nivel de base de datos que el ledger es append-only, coherente con el diseño de v0.8); `audit_log` abierto a cualquier vendedor/master activo.
  - **Verificado end-to-end** con un script que simula exactamente la secuencia de `saveSale`: crear venta con 2 items y pago fraccionado en 2 métodos → editar agregando un 3er item y **corrigiendo un pago mal cargado** (el escenario crítico de v0.8) → confirmar que el pago viejo desaparece y el nuevo aparece, que el neto en Caja sigue siendo exacto (sin dinero fantasma) → togglear estado de item y proceso → verificar `audit_log` → limpieza sin dejar rastro. Los 8 pasos pasaron.
  - **Nota de proceso**: el SQL de esta tanda (RLS + trigger de folio) fue el más grande hasta ahora y volvió a toparse con el problema de pegado de la caja de texto del SQL Editor viejo conocido — se resolvió partiéndolo en 6 bloques chicos y, cuando aun así falló una política puntual (`pagos_insert`, con el error real "row-level security policy" en vez de un error de sintaxis), re-ejecutando esa parte de nuevo (las guardas `do $$ ... exception when duplicate_object then null; end $$;` hacen que sea siempre seguro repetir cualquier bloque).
- **2026-07-16** (noche, cont.) — v1.2: **`cajas` migrada a Supabase**, siguiendo el orden corregido (cajas antes que pedidos/pagos, porque estos últimos tienen `caja_id` como FK hacia `cajas` — migrarlos en el orden original hubiera hecho fallar cada `insert` con una violación de foreign key). `openCaja`/`closeCaja` pasaron a `async`, hablan con Supabase, y se agregó `loadCajas()` (mismo patrón que `loadVendors`/`loadClients`, con `.select("*, vendedores(name)")` para traer el nombre del vendedor por join en vez de guardarlo duplicado). El cálculo del cuadre (`expectedCashUSD`) sigue leyendo del ledger `payments` en memoria — es un efecto secundario temporal documentado en Pendientes, se resuelve solo al migrar `pagos`. **RLS**: nueva función reutilizable `my_vendedor_id()` (security definer, devuelve el `vendedores.id` del usuario autenticado — se va a volver a usar para `pedidos`/`pagos`) + políticas: cada vendedor ve/abre/cierra solo su propia caja, el master ve todas pero no puede abrir/cerrar (coincide con la regla de negocio ya documentada de que el master no opera caja propia). **Verificado end-to-end**: abrir caja como vendedor → confirmar que el master la ve → cerrarla con cuadre calculado → limpieza sin dejar basura.
- **2026-07-16** (noche) — v1.1: **primera pieza del núcleo migrada a Supabase: `clientes`**, siguiendo el orden sugerido en v1.0 ("empezar chico"). `findOrCreateClient` pasó de mutar un array en memoria a ser `async`: busca por teléfono (o por nombre exacto si no hay teléfono) con `.maybeSingle()`, actualiza los campos si el cliente ya existe o inserta uno nuevo, y sincroniza el cache local `clients` (usado por `ClientesScreen`/`ClienteDetail`) con lo que devuelve Supabase. `saveSale` pasó a ser `async` para poder esperar ese `await findOrCreateClient(...)`. Se agregó `loadClients()` (mismo patrón que `loadVendors`) que trae todos los clientes al iniciar sesión. **RLS real para `clientes`**: se agregó la función `is_active_vendedor` (mismo patrón `security definer` que `is_active_master`) y políticas de select/insert/update para cualquier vendedor o master **activo** — a diferencia de `vendedores`, acá no se restringe por dueño: el directorio de clientes es compartido entre todo el staff (evita duplicar el mismo cliente si lo atiende un vendedor distinto la próxima vez), coherente con el modelo de permisos ya documentado. **Verificado end-to-end** con un script que hace exactamente lo que hará la app: login vendedor → insert → buscar por teléfono → update → confirma que un cliente anónimo (sin login) sigue sin poder ver nada. **Advertencia documentada:** como `pedidos`/`sales` todavía están en memoria, la pantalla "Clientes" (que filtra por clientes-con-venta-visible) no muestra clientes de sesiones anteriores hasta que también se migre esa parte — ver Pendientes.
- **2026-07-16** (tarde/noche) — v1.0.1: **Edge Function `create-vendor` deployada y verificada end-to-end**. El usuario corrió `npx supabase login && npx supabase link --project-ref aqtaphhhomlbafelekiz && npx supabase functions deploy create-vendor` de su lado; se confirmó con un script one-off que hace exactamente lo que hace la app real: login como master → `supabase.functions.invoke("create-vendor", {...})` → el vendedor de prueba queda creado (fila en `vendedores` + cuenta de Auth) → ese vendedor nuevo puede loguearse de inmediato → limpieza del registro de prueba (nunca quedó basura en la base). El manager de apertura queda 100% funcional, sin pendientes.
- **2026-07-16** — v1.0: **primera conexión real a Supabase** — login/roles y el "manager de apertura" de vendedores dejaron de ser en memoria. (1) **Proyecto real**: se creó el proyecto en supabase.com, se corrió `schema.sql` contra él (ya estaba parcialmente corrido de una sesión anterior del usuario — se detectó y solo se aplicó el delta: columna `auth_user_id` nueva en `vendedores`), y se instaló `@supabase/supabase-js` con un cliente Singleton en `src/lib/supabase.js` (guardado en `globalThis` para sobrevivir al HMR de Vite). `.env`/`.env.example` agregados, `.gitignore` ampliado para nunca commitear credenciales. (2) **Login real**: `MASTER` hardcodeado eliminado; `login()` ahora llama `supabase.auth.signInWithPassword` con un email sintético (`cedula@zappostore.local`, invisible para el usuario) y despues busca el perfil/rol en `vendedores` por `auth_user_id`. Se agregó restauración de sesión al refrescar (`useEffect` + `supabase.auth.getSession()`) y una pantalla de carga corta mientras se verifica. Se sacó el viejo comportamiento de auto-crear un vendedor con cualquier cédula tipeada (agujero de seguridad con auth real). Verificado con un script que simula exactamente la lógica de `login()` contra la base real: 4 casos (master OK, vendedor OK, y los 2 cruces de credenciales-en-pantalla-equivocada correctamente rechazados). (3) **Manager de apertura**: como el proyecto tiene self-signup deshabilitado a nivel de instancia (correcto para un sistema interno), el alta de vendedores se resolvió con una **Edge Function** (`supabase/functions/create-vendor`) que usa la Service Role Key solo del lado del servidor, valida que quien llama sea un master activo, crea el usuario de Auth y la fila en `vendedores` de forma atómica (con rollback si falla el insert). `VendedoresScreen`/`addVendor`/`toggleVendor` en `App.jsx` ahora leen/escriben Supabase real en vez del array en memoria. (4) **Seguridad**: se detectó que, antes de este cambio, la anon/publishable key (pública por diseño, va en el bundle del navegador) podía leer y escribir **todas** las tablas sin ninguna restricción — no había RLS habilitado en ningún lado. Se aplicó una política RLS base: `vendedores` con políticas reales (cada uno lee su propia fila; un master activo lee/actualiza todas, vía una función `security definer` para evitar el clásico problema de recursión de RLS auto-referencial) y el resto de las tablas con RLS habilitado sin políticas (bloqueo total, ya que la app todavía no las usa — políticas granulares por rol quedan para cuando se migren esas pantallas). (5) **2 cuentas sembradas** (master + vendedor) vía un script Node one-off con la Service Role Key, nunca commiteado ni guardado en el repo — se usó y se descartó en el momento. El RLS se aplicó desde el SQL Editor del dashboard (no pudo pegarse en un solo bloque por problemas de la caja de texto del usuario reteniendo contenido de intentos previos; se resolvió partiéndolo en sentencias de una sola línea con guardas `do $$ ... exception when duplicate_object then null; end $$;` para que fueran seguras de re-ejecutar). **Verificado después de aplicado:** se re-corrió la simulación de los 4 casos de login (sigue igual) y se confirmó con `curl` sin credenciales que `vendedores`/`clientes`/`pedidos` ahora devuelven `[]`. **Fuera de esta tanda, a propósito:** Vender/Seguimiento/Caja/Clientes/Resumen/Reportes siguen 100% en memoria; migrarlos es la próxima etapa grande (ver Pendientes).
- **2026-07-15** — v0.9: refactor de UI/lógica de visualización de `ComandaModal` (sin tocar `saveSale`, `cajaTotals` ni el estado de `App`) + alineación completa de `schema.sql` al modelo actual, como paso previo a levantar Supabase. **Comanda:** (1) los procesos de Taller/Diseño de cada ítem ahora se renderizan como checklist real (`ProcesoChecklist`, nuevo componente) cruzando `procesos_taller`/`procesos_diseno` con sus `_done` — `Check` verde si está completado, `Clock` gris si sigue pendiente, en vez del texto plano de antes. (2) Bloque de cliente destacado (`bg-slate-50 border rounded-lg`) con Nombre/Cédula/Teléfono/Dirección o "Cliente Ocasional" si no hay datos, reemplazando las líneas sueltas que había. (3) Bloque de pago rehecho como recibo formal: Total Pedido (USD+Bs), Detalle de Abonos (uno por línea de `orderPayments` con método/referencia/monto), Total Pagado, y un recuadro final "Pagado Completo" (verde) o "SALDO PENDIENTE" (rojo, con el monto exacto en USD y Bs) — antes solo había una línea de "Estado del pago". (4) Impresión térmica real: `<style>` local con `@media print` + `@page { size: 58mm auto; margin: 0 }`, que aísla el contenido de la comanda (`.zs-print-ticket`) del resto del documento con el truco `visibility:hidden`/`visibility:visible`, achica todo el texto a 9–12px (`!important` para pisar las clases de Tailwind que ya traía cada elemento), limita las fotos a 36px, y usa `print:hidden` para ocultar header/tabs/botones al imprimir. La vista en pantalla no cambió (mismo modal, bordes, colores). **Schema:** `schema.sql` pasó de v0.3 a v0.4 — se comparó campo por campo contra el modelo real en memoria y se realinearon `pedido_items` (sin `formato`/`peso_kg`; `operacion_tipo` + `procesos_taller`/`procesos_diseno` + `*_done` en vez de `destino_tipo`/`destinos`), se agregaron `pedido_item_diseno` y `pedido_item_fotos` (diseño por zona, reemplazan el viejo `photo_path`/`notes` único), se agregó `cedula`/`direccion` a `clientes`, se rediseñó `pagos` con `order_payment_id`+`is_reversal` para soportar la reversión de pagos corregida en v0.8, se quitó `pay_mode` y `subtotal` de `pedidos` (no existen en memoria), y se agregó la vista `v_pedido_saldo`. **Verificado:** `npm run build` limpio tras el cambio de `ComandaModal`; `schema.sql` se validó con un chequeo de balance de paréntesis y una revisión manual del orden de dependencias entre tablas/tipos (no se pudo correr contra un Postgres real: sin `psql` ni Docker disponibles en este entorno — queda pendiente probarlo contra Supabase antes de aplicarlo).
- **2026-07-15** — v0.8: auditoría y fix de una **fuga de dinero** en el ledger de pagos, a pedido explícito del cliente antes de seguir avanzando. Se detectó que, al editar una venta ya guardada, **quitar un abono que ya estaba registrado** (ej. corregir un monto mal tipeado) no revertía su evento correspondiente en el ledger de Caja (`payments`) — el monto viejo quedaba como "dinero fantasma" para siempre, generando un descuadre imposible de rastrear al cerrar turno (ej.: cargar $300 por error en vez de $30, corregirlo, y que Caja igual sume $330 cobrados sobre una venta de $30). **Fix:** `makePaymentEvent` ahora incluye `orderPaymentId` (vincula cada evento del ledger a la entrada de `orderPayments` que lo generó); `saveSale` calcula el diff agregado/quitado comparando `orderPayments` viejo vs. nuevo por `id`, y al detectar un pago quitado busca su evento original en el ledger (por `orderPaymentId`) para generar un **evento de reversión negativo en la MISMA caja donde se cobró originalmente** (no en la caja actual de quien edita). También se agregó un resumen de pagos agregados/anulados al `audit_log` de cada edición, para trazabilidad. **Verificación:** se escribió un script de simulación standalone en Node (`payment_sim.mjs`, no forma parte del repo) que replica exactamente `payConverted`/`makePaymentEvent`/`cajaTotals`/`saveSale` y corre 8 escenarios — fraccionado en 2 y 3 métodos con decimales feos, pago único en Efectivo $ (USD) y en Pago Móvil (VES), pago parcial (no debe marcarse como pagado), el escenario crítico de corregir un pago mal cargado, descartar un pago antes de guardar (no debe dejar rastro), y re-mezclar métodos en la misma edición — los 8 pasan sin ninguna discrepancia de dinero. `npm run build` limpio. No se hizo verificación visual click-a-click porque el entorno de esta sesión no tiene `chromium-cli`/Playwright disponibles; se dejó el servidor (`npm run dev`) corriendo en background para que el usuario probara en `http://localhost:5173`.
- **2026-07-15** — v0.7: refactor crítico de lógica de negocio y UI en `App.jsx`, previo a migrar a SQL. (1) **Pago mixto por pedido**: se eliminó el pago por ítem y el switch "Todo junto/Fraccionado"; nuevo componente `PaymentPanel` con `orderPayments: []` — se agregan N abonos con métodos distintos, indicador de total en USD/Bs, lista de abonos con opción de quitar, y "Restante por pagar" en tiempo real (verde "Pagado completo" al llegar a $0). (2) **Producto simplificado**: se eliminaron `FORMATOS` (talle/peso/cantidad) y `COLORES` (paleta fija); ahora es solo Talla (chips) + Cantidad (input limpio, sin botones +/-) + Color como texto libre. (3) **Diseño multi-foto por zona**: `draft.designData = { frente, espalda, mangas }`, cada uno con `photos: []` (multi-carga, miniaturas, botón "X") y `notes` independiente; `ZonePicker` pasó a ser controlado desde `ItemEditForm` (antes manejaba su propio tab interno) para poder mostrar el conteo de fotos en el título de cada pestaña (ej. "Espalda (2 📸)"); la comanda impresa se actualizó para listar fotos/notas agrupadas por zona en vez de una sola foto/nota por producto. (4) **Cliente**: se agregaron campos Cédula/RIF y Dirección, persistidos por `findOrCreateClient`. (5) **Presupuesto**: se reforzó con un guard defensivo extra dentro de `saveSale` (además del control de UI ya existente) que corta cualquier intento de guardar una venta con productos en estado "presupuesto". **Verificado** con `npm run build` (sin errores) y un `grep` exhaustivo para confirmar que no quedó ninguna referencia colgante a los campos/identificadores eliminados (`payMode`, `orderPay`, `FORMATOS`, `COLORES`, `.formato`, `.peso`, `it.photo`/`it.notes`). **Encontrado en la revisión posterior (ver v0.8):** el diff add-only de pagos de esta versión no revertía pagos quitados durante una edición — corregido en v0.8.
- **2026-07-14** — v0.6: se activaron dos módulos que estaban en "Próximamente" en el sidebar, ambos **solo visibles para el rol master** (`NAV`, antes en `NAV_SOON`). (1) **Resumen Financiero** (`ResumenFinancieroScreen`): 3 tarjetas KPI —Ingresos Totales histórico (suma de `payments[].amountUSD`, el mismo ledger que usa Caja), Cuentas por Cobrar (suma de `sale.data.balance` de pedidos con saldo pendiente) y Ticket Promedio (total vendido / cantidad de ventas)— más una lista de Cuentas por Cobrar con folio, cliente y monto pendiente, ordenada de mayor a menor deuda. (2) **Reportes / Estadísticas** (`ReportesScreen`): gráfico de ingresos de los últimos 6 meses reutilizando el componente `BarChart` (SVG propio, ya usado en las fichas de cliente/vendedor — el proyecto no tiene librería de gráficos instalada), ranking Top 5 **Mejores Clientes** y Top 5 **Mejores Vendedores** por volumen en USD, con filas clicables que navegan a `ClienteDetail`/`VendedorDetail` (mismo patrón que el resto de la app). Ambos son pantallas de solo lectura: no agregan campos nuevos al modelo de datos, solo agregan `sales`/`payments`/`clients`/`vendors` que ya existían en memoria. Verificado con `npm run build` (sin errores) y arrancando el dev server; no se hizo verificación click-a-click en navegador por falta de herramienta de automatización en el entorno de esa sesión — pendiente que el usuario confirme visualmente. **Pendiente:** los KPIs son históricos globales, no filtran por fecha/turno (ver nota en Pendientes).
- **2026-07-11** — v0.5: actualización arquitectónica mayor hacia **Dashboard de gestión de producción (ERP/Kanban)**. (1) **Layout**: `Sidebar` pasó de drawer/overlay a columna fija (`<aside>` w-72 dentro de un `flex h-screen`); se eliminó el botón hamburguesa de `TopBar` y toda la lógica de abrir/cerrar (`drawer`, `openMenu`, `onClose`, `X`). (2) **Modelo de datos del producto**: se eliminó el array `destinos` (`venta`/`taller`/`diseño`) y se reemplazó por `tipo_operacion` ("venta"|"presupuesto"), `procesos_taller` y `procesos_diseno` (qué procesos aplican) más `procesos_taller_done`/`procesos_diseno_done` (cuáles ya se completaron). `ItemEditForm` ahora tiene 3 acordeones (Operación / Taller / Diseño) en vez del selector de Destinos; ya no es obligatorio elegir nada para guardar un producto. (3) **Presupuesto vs. Venta**: si hay un producto con `tipo_operacion="presupuesto"` en el carrito, el botón inferior de Vender se vuelve "Imprimir Presupuesto" (azul) y, al usarse, abre la comanda en modo solo-imprimir (reutilizando el `readOnly` de `ComandaModal`) sin invocar `onSaved` — no crea pedido, no toca Caja. (4) **Kanban de producción en Seguimiento**: "Gestionar productos" ahora muestra, por producto, 2 bloques (Taller/Diseño) con los procesos elegidos como chips clicables que alternan pendiente (gris) ↔ completado (verde) vía el nuevo handler `toggleProceso` en `App`, dejando rastro en `audit_log`. La comanda impresa también se reagrupó (`grupos`: Pedido/Taller/Diseño en vez de por Destino) y muestra los procesos de cada item. Verificado end-to-end con Playwright (login, agregar producto con procesos, guardar venta, togglear chip en Seguimiento, flujo de presupuesto completo) sin errores de consola. **Pendiente:** `schema.sql` no se tocó — sigue con el `destino_tipo` viejo, hay que migrar el modelo cuando se conecte Supabase.
- **2026-07-11** — v0.4.1: ajuste de la pestaña Espalda de `ZonePicker`. Se sacó "Esp. Completa" del dibujo/SVG (`DESIGN_VIEWS.espalda.zones` quedó con las 3 zonas físicas: alta/media/baja); el 4º botón sigue existiendo solo en la lista, con estado 100% calculado a partir de si las 3 partes están presentes (`ESPALDA_PARTS`, constante compartida entre `ZonePicker` y `toggleZone`). Esto resolvió de forma bidireccional el pendiente que había quedado abierto en v0.4: tocar "Esp. Completa" ahora marca/desmarca las 3 partes en ambos sentidos, sin asimetría.
- **2026-07-11** — v0.4: rediseño del selector de **Zonas de diseño** (`ZonePicker`/`DESIGN_VIEWS` en `App.jsx`). Pasó de mostrar frente+espalda fijos con costados sueltos a un componente con **3 pestañas** (Frente / Espalda / Mangas), nomenclatura textil profesional y **visión de espejo** (la izquierda del dibujo es la derecha real de la prenda). Cada vista muestra un dibujo (`ShirtSVG` para frente/espalda, nuevo `ShirtSideSVG` de perfil para mangas) con círculos numerados clicables sobre las zonas, más una lista en grid debajo para tocar sin apuntar. Se agregó `ZONE_LABELS` (mapa plano derivado de `DESIGN_VIEWS`) para no romper el label de zonas en la comanda impresa. Nueva regla de negocio en `toggleZone` (dentro de `ItemEditForm`): marcar alta+media+baja de espalda las fusiona automáticamente en "Esp. Completa"; marcar "Esp. Completa" limpia las 3 partes. Pendiente de decidir: si desmarcar "Esp. Completa" debería re-expandir a las 3 partes. Cambio hecho de forma iterativa junto al cliente (varias rondas de ajuste de coordenadas/SVG) antes de tocar Supabase — sigue 100% en memoria.
- **2026-07-10** — v0.3: módulo **Caja** completo. Ledger de pagos por evento (`payments`) reemplaza el campo "pagado" acumulado como fuente de verdad para "cuánto entró hoy" — se generan automáticamente al guardar una venta nueva o un abono, calculando el delta contra el pago previo. Apertura/cierre de turno (`cajas`) con fondo inicial en efectivo, cuadre esperado-vs-contado al cerrar (sobrante/faltante). `CajaScreen`: vista vendedor (abrir/cerrar turno, 5 indicadores del turno — vendido, cobrado por método, dinero asegurado, señas en curso, comprometido por cobrar — ventas del turno, historial propio por día/semana/mes) y vista master (supervisión global: turnos abiertos de todos los vendedores + historial global con drill-down al cuadre de cada turno). `schema.sql`: tabla `cajas` ampliada (fondo/cierre/tasa/esperado/varianza) y movida antes de `pedidos`; `caja_id` agregado a `pedidos` y `pagos`.
- **2026-07-08** — v0.2: roles + login master aparte, menú lateral tipo Fina (con "Próximamente"), clientes auto-registrados, historiales cliente/vendedor con mini-gráfico, gestión de vendedores, edición de pedidos en cualquier estado + audit log visible, regla de cierre de venta, pago fraccionado/junto, precio fijo en peso. Logo transparente definitivo + wordmark rojo LED suave. Esquema SQL ampliado (vendedores, clientes, pedidos, items, pagos, audit_log, vistas, ganchos stock/caja). Empaquetado como proyecto Vite.
- **(previo)** — v0.1: esquema inicial + POS receptor de pedidos + Kanban conceptual; iteraciones de UI (Fina-like), logo del sapo, wordmark.
